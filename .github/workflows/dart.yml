name: Build and Release APK

on:
  push:
    branches:
      - master

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest

    env:
      RELEASE_KEYSTORE_PHRASE: ${{ secrets.RELEASE_KEYSTORE_PHRASE }}
      RELEASE_KEYSTORE_FILE: ${{ secrets.KEYSTORE_FILE }}
    
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Set up Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.19.5'

    - name: Install Dependencies
      run: flutter pub get

    - name: Set GPG_TTY environment variable
      run: echo "export GPG_TTY=true" >> $GITHUB_ENV


    - name: Decrypt Keystore File
      run: |
        echo "${{ KEYSTORE_FILE }}" > encrypted_keystore.gpg
        echo "${{ RELEASE_KEYSTORE_PHRASE }}" > keystore_passphrase.txt
        gpg -d --passphrase-file=keystore_passphrase.txt -o keystore.keystore encrypted_keystore.gpg

        # echo "${{ secrets.KEYSTORE_FILE }}" > encrypted_keystore.keystore.asc
        # gpg -d --passphrase "${{ secrets.RELEASE_KEYSTORE_PHRASE }}" --batch encrypted_keystore.keystore.asc > app/release.keystore


    - name: Build APK
      run: flutter build apk --release

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v2
      with:
        name: app-release
        path: build/app/outputs/flutter-apk/app-release.apk
